atoi
====
```c
#include<stdlib.h>
#include<cstdio.h>

int main(){
        int num = 0;
        char *str = "2022";
        printf("*char = %s\n", str);
        num = atoi(str);
        printf("int = %d\n", num);
        return 0;
}

// character to integer
```
